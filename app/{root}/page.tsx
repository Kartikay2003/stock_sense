import {Button} from "@/components/ui/button";

const Home = () => {
    return (
        <div className="flex min-h-screen home-wrapper">
            {// ctrl + click on custom class -> to see what it does (will open that class in CSS)
            }
            Home
        </div>
    )
}
export default Home
